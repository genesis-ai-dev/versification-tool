# FRVT Web

React + TypeScript + Vite SPA for the versification viewer.

Build output lands in `dist/` and is served by FastAPI with an SPA route fallback.

```bash
npm install
npm test
npm run typecheck
npm run lint
npm run format:check
npm run build
```
